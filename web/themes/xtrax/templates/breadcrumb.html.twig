{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  <div class="row">
    <div class="col-lg-12 col-md-12 whitecolor">
      <h3 class="float-left" style="text-transform:capitalize;">{{ page_title ? page_title : breadcrumb|last.text }}</h3>
      {# <ul class="breadcrumb top10 bottom10 float-right">
        {% for item in breadcrumb %}
          {% if item.url %}
            <li class="breadcrumb-item hover-light"><a href="{{ item.url }}">{{ item.text }}</a></li>
          {% else %}
            <li style="text-transform:capitalize;"class="breadcrumb-item hover-light">{{ item.text }}</li>
          {% endif %}
        {% endfor %}
      </ul> #}
    </div>
  </div>
{% endif %}
